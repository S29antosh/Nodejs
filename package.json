{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "server": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemon": "^2.0.22"
  }
}
